cat .git/config - выводит содержимое файла

cat ~/.gitconfig - email, name

git config --global core.editor notepad - вызов блокнота, дл€ записи коммита при:

git commit - сохранение в репозиторий, с вызовом текстового редактора

git config --global core.editor - просмотр выбранного блокнота дл€ коммита

git config alias.c '!git ...; git ...' - несколько комманд в алиасе

git config alias.c '--global' - одна комманда в алиасе

git config --list - вывести настройки


less - листалка в терминале

/editor - поиск в тексте "editor"

n - поиск вперЄд

shift + n - поиск назад


git log - посмотреть список коммитов

git update-index --chmod=+x <file.name> - изменение прав исполнимости файла в индексе(дл€ unix)

git add --chmod=+x <file.name> - изменение прав исполнимости файла не добавленного в index (дл€ unix)

git show - посмотреть текущий коммит

git show 293ada3e4a70604ccc2c833e5b7ecc4300f525b8 - посмотреть коммит

git show --pretty=fuller - посмотреть больше инф о коммите

git commit --author='Ded Inside <ded@.com>' --date='4' - коммит с автором и временем

git init - ѕозвол€ет проинициализировать репозиторий в текущей папке

git status - ѕоказывает текущий статус

git restore --staged redactor - откатить добавление в git(index) всех файлов из папки redactor

git add - ќтслеживает изменени€ файлов

git add --force redactor/rrr.txt - добавить файл из .gitignore

git add index.html Ч добавл€ет index.html

git add . Ч добавл€ет все файлы

git commit - —охран€ет изменени€ в коммит

git commit -m 'commit message' Ч создает коммит с сообщением

git branch Ч показывает список веток

git branch branch-name Ч создает новую ветку branch-name

git branch -D branch-name Ч удал€ет ветку branch-name

git checkout - ѕереключаетс€ на другую ветку

git checkout branch-name Ч переключаетс€ на последний коммит в ветке branch-name

git checkout -b branch-name Ч создает и переключаетс€ на ветку branch-name
 
git merge - —овмещает текущую ветку с выбранной

git merge branch-name Ч совмещает текущую ветку с branch-name

git config -  онфигураци€ и параметры git

git config --global user.name Ч ѕоказывает им€ пользовател€

git config --global user.name 'new user' Ч »змен€ет им€ пользовател€

git config --global user.email Ч ѕоказывает email пользовател€

git config --global user.email 'test@mail.ru' Ч »змен€ет email пользовател€

git push - «аливает текущие локальные коммиты в удаленный репозиторий

git pull - «абирает изменени€ с удаленного репозитори€ в локальный

git clone -  лонирует проект с удаленного репозитори€

git push --set-upstream origin СnameТ        заливает побочную ветку

